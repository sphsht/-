public class Kata {
  public static String createPhoneNumber(int[] numbers) {
  String num = "(xxx) xxx-xxxx";
  
  for(int i = 0; i < numbers.length; i++)
  {
    num =num.replace('x', numbers[i]);
  }
  
  return format;
     
  }
}
